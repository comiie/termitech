# KZ08TEPAR0921 电控连接器拆解演示

基于原始 STEP 模型构建的交互式 3D 网页，支持旋转、缩放、零件点选、自动旋转以及一键拆解/复原。

## 本地运行

```bash
pnpm install
pnpm dev
```

## 重新转换 STEP 模型

```bash
pnpm convert-model /absolute/path/to/model.step
```
