# 晨昏线官网

基于 Figma 节点 `176:2` 的静态首页还原。

## 本地运行

```bash
npm install
npm run dev
```

## 实现约定

- 设计基准为 1920 × 8116，1080px 对应设计稿一屏。
- 页面随视口宽度等比适配，1920 × 1080 下与设计稿 1:1 对齐。
- 当前版本不包含入场、悬停、视差等动效，只保留下滑阻尼。
- 原生滚动位置保留，右侧滚动条隐藏；键盘、触控板和触摸滚动均可用。
